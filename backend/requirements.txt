# nlpl_status backend — runs the vendored EOD engine (backend/engine).
# Core framework
flask>=3.0.0,<4.0
flask-cors>=4.0.0

# Data processing
pandas>=2.0.0,<3.0
numpy>=1.24.0
duckdb>=0.9.0,<2.0
pyarrow>=15.0.0,<22.0.0

# Excel I/O
openpyxl>=3.1.0,<4.0
xlsxwriter>=3.1.0,<4.0
python-calamine>=0.2.0,<1.0
pyxlsb>=1.0.10,<2.0  # read month-end OD .xlsb files (OD Report FTOD step)

# Memory monitoring
psutil>=5.9.0,<7.0

# HTTP client (auto-upload to EC2 / Supabase)
requests>=2.28.0

# Production WSGI server (cross-platform; works on Windows unlike gunicorn)
waitress>=2.1.0

# WhatsApp Web automation (optional — only for the WhatsApp-send feature).
# After install, run once: python -m playwright install chromium
playwright>=1.40.0
